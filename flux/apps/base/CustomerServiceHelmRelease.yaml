apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: customer-service
  namespace: default
spec:
  dependsOn:
    - name: kafka
  chart:
    spec:
      chart: customer-service
      sourceRef:
        kind: HelmRepository
        name: application
      version: "0.1.0-BUILD.20260102063834" # {"$imagepolicy": "default:customer-service-image-policy:tag"}
  interval: 15s
