apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: order-service
  namespace: default
spec:
  dependsOn:
    - name: kafka
  chart:
    spec:
      chart: order-service
      sourceRef:
        kind: HelmRepository
        name: application
      version: "1.3.0"
  interval: 15s
