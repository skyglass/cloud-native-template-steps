apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: api-gateway-service
  namespace: default
spec:
  dependsOn:
    - name: ingress-nginx
      namespace: ingress-nginx
  chart:
    spec:
      chart: api-gateway-service
      version: "0.1.0-BUILD.20260101155305" # {"$imagepolicy": "default:api-gateway-service-image-policy:tag"}
      sourceRef:
        kind: HelmRepository
        name: application
  interval: 15s
