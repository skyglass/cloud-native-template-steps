apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: api-gateway-service
  namespace: default
spec:
  chart:
    spec:
      version: "0.1.0-BUILD.20260102152500" # {"$imagepolicy": "default:api-gateway-service-image-policy:tag"}
  values:
    replicaCount: 2
