apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: customer-service
  namespace: default
spec:
  chart:
    spec:
      version: "0.1.0-BUILD.20260101133914" # {"$imagepolicy": "default:customer-service-image-policy:tag"}
