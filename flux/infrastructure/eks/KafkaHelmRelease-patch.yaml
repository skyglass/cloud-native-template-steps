apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kafka
  namespace: default
spec:
  values:
    kafka:
      persistence:
        storageClassName: kafka-sc
    zookeeper:
      persistence:
        storageClassName: kafka-sc
