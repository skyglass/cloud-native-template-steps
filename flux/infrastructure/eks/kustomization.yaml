apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kafka-storage.yaml
  - ../base/KafkaHelmRelease.yaml  # Include original Kafka HelmRelease so patch can apply
patches:
  - path: ./KafkaHelmRelease-patch.yaml